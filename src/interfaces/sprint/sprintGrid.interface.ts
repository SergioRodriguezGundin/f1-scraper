import { SprintGrid } from '../../xata';

export type SprintGridKeys = keyof SprintGrid;

export const sprintGridKeys: SprintGridKeys[] = ['id', 'position', 'driverNumber', 'driver', 'team', 'time', 'place', 'year'];
